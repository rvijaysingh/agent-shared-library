"""
Prompt template loader.

Loads markdown template files from a caller-specified directory and performs
variable substitution using {placeholder} syntax. This library ships no
prompt files — the consuming agent owns its prompts/ directory.
"""

# TODO: Implement PromptLoader (Phase 4)
