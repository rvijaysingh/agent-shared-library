"""Integration tests simulating consumer usage patterns.

Tests complete workflows end-to-end using mock data:
- Config load -> LLM call -> Trello card creation
- Config load -> card reads -> LLM classification -> card move
All external dependencies (Anthropic, Ollama, Trello) are mocked.
"""

# TODO: Implement integration tests (Phase 5)
